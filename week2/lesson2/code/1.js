// Условный оператор "?"

let age = +(prompt("Введите свой возраст: ")); // <= значение будет строка

console.log("Тип age " + typeof(age));

let message = (age > 18) ? "Вы можете купить алкоголь" : "Вам запрещено!";

let check = (age > 18);

console.log(check);